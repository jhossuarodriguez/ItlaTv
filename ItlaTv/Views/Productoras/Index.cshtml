@model IEnumerable<ItlaTv.Models.Productora>

<h2 class="mt-4 mb-4">Listado de Productoras</h2>

<a asp-action="Create" class="btn btn-primary">Crear Nueva Productora</a>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var productora in Model)
        {
            <tr>
                <td>@productora.Nombre</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@productora.Id" class="btn btn-warning">Editar</a>
                    <a asp-action="Delete" asp-route-id="@productora.Id" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>